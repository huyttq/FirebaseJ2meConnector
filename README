1. You will need download and install Netbeans and J2ME SDK 3.05 to build this library.
2. The master branch includes Firebase native encryption part. However it is not compatible with Firebase Server,
   you may need to switch to use BouncyCastle lib instead of J2SE.
3. The lightweight branch does not implement native encryption but you may find it work with socket ssl.
